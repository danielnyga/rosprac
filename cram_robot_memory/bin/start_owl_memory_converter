#!/bin/bash

# This script is necessary to start the service in the robot_memory package
# That in turn is necessary to get a defined path for the owl files
# since roslaunch does not allow to start a script in a foreign package

rosrun owl_memory_converter converter/build/install/converter/bin/converter
